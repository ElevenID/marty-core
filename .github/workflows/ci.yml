name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1
  # Bypass rustup's implicit component installation from rust-toolchain.toml;
  # each job installs its required components explicitly.
  RUSTUP_TOOLCHAIN: 1.97.1

jobs:
  release-contract:
    name: Release Contract
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: '3.12'
      - name: Verify Python versions and release-asset retention
        run: |
          python scripts/check_release_contract.py
          python -m unittest discover -s scripts -p "test_*.py"

  test:
    name: Test
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        rust: [stable]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Install Rust ${{ matrix.rust }}
        uses: dtolnay/rust-toolchain@2c7215f132e9ebf062739d9130488b56d53c060c
        with:
          toolchain: ${{ matrix.rust }}

      - name: Cache cargo registry
        uses: actions/cache@caa296126883cff596d87d8935842f9db880ef25
        with:
          path: ~/.cargo/registry
          key: ${{ runner.os }}-cargo-registry-${{ hashFiles('**/Cargo.lock') }}

      - name: Cache cargo index
        uses: actions/cache@caa296126883cff596d87d8935842f9db880ef25
        with:
          path: ~/.cargo/git
          key: ${{ runner.os }}-cargo-index-${{ hashFiles('**/Cargo.lock') }}

      - name: Cache cargo build
        uses: actions/cache@caa296126883cff596d87d8935842f9db880ef25
        with:
          path: target
          key: ${{ runner.os }}-${{ runner.arch }}-${{ env.RUSTUP_TOOLCHAIN }}-cargo-build-target-${{ hashFiles('**/Cargo.lock') }}

      - name: Require reproducible release sources
        shell: bash
        run: |
          test -f Cargo.lock
          test -f marty-test-wallet/Cargo.toml
          cargo metadata --locked --no-deps --format-version 1 >/dev/null

      - name: Run tests (workspace, excluding native extension crates)
        run: cargo test --locked --workspace --exclude marty-zkp --exclude marty-bindings --features test-fixtures

      - name: Run binding tests without extension-module linking
        run: cargo test --locked -p marty-bindings --no-default-features

      - name: Run marty-zkp tests (ZK mock)
        env:
          USE_ZK_MOCK: "1"
        run: cargo test --locked -p marty-zkp

  clippy:
    name: Clippy
    runs-on: ubuntu-latest
    env:
      USE_ZK_MOCK: "1"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4
        with:
          toolchain: 1.97.1
          components: clippy
      - name: Run clippy
        run: cargo clippy --locked --workspace --features test-fixtures -- -D warnings

  fmt:
    name: Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4
        with:
          toolchain: 1.97.1
          components: rustfmt
      - name: Check formatting
        run: cargo fmt --all -- --check

  security:
    name: Security Audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4
        with:
          toolchain: 1.97.1

      - name: Cache cargo-audit
        uses: actions/cache@caa296126883cff596d87d8935842f9db880ef25
        with:
          path: ~/.cargo/bin/cargo-audit
          key: ${{ runner.os }}-cargo-audit

      - name: Install cargo-audit
        run: |
          if ! command -v cargo-audit &> /dev/null; then
            cargo install cargo-audit --locked
          fi

      - name: Run cargo-audit
        continue-on-error: true
        run: cargo audit

      - name: Cache cargo-deny
        uses: actions/cache@caa296126883cff596d87d8935842f9db880ef25
        with:
          path: ~/.cargo/bin/cargo-deny
          key: ${{ runner.os }}-cargo-deny

      - name: Install cargo-deny
        run: |
          if ! command -v cargo-deny &> /dev/null; then
            cargo install cargo-deny --locked
          fi

      - name: Run cargo-deny
        continue-on-error: true
        run: cargo deny check

  python-security:
    name: Python Security (marty-biometrics)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: marty-biometrics
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: '3.12'

      - name: Install security tools
        run: |
          python -m pip install --upgrade pip
          pip install bandit[toml] safety

      - name: Run bandit
        continue-on-error: true
        run: bandit -r python/marty_biometrics/

      - name: Run safety check
        continue-on-error: true
        run: safety check

  feature-matrix:
    name: Feature Matrix (${{ matrix.crate }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        crate:
          - marty-crypto
          - marty-secure-storage
          - marty-bindings
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4
        with:
          toolchain: 1.97.1

      - name: Test no default features
        run: cargo test --locked -p ${{ matrix.crate }} --no-default-features

      - name: Exercise default features
        shell: bash
        run: |
          # PyO3's extension-module feature intentionally omits libpython
          # linkage, so it must be compiled rather than linked into a Rust
          # test executable. Binding tests run separately with that feature
          # disabled, and Python-package jobs test the actual extension.
          if [ "${{ matrix.crate }}" = "marty-bindings" ]; then
            cargo check --locked -p marty-bindings
          else
            cargo test --locked -p "${{ matrix.crate }}"
          fi

  feature-matrix-verification:
    name: Feature Matrix (marty-verification)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4
        with:
          toolchain: 1.97.1

      - name: Build no default features
        run: cargo build --locked -p marty-verification --no-default-features

      - name: Build with default features
        run: cargo build --locked -p marty-verification

      - name: Run tests with fixtures
        run: cargo test --locked -p marty-verification --features test-fixtures

  feature-matrix-oid4vci:
    name: Feature Matrix (marty-oid4vci)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4
        with:
          toolchain: 1.97.1

      - name: Check no default features
        run: cargo check --locked -p marty-oid4vci --no-default-features

      - name: Test default features
        run: cargo test --locked -p marty-oid4vci

      - name: Check issuer role alone
        run: cargo check --locked -p marty-oid4vci --no-default-features --features issuer

      - name: Check verifier role alone
        run: cargo check --locked -p marty-oid4vci --no-default-features --features verifier

  feature-combinations:
    name: Feature Combinations
    runs-on: ubuntu-latest
    env:
      MATRIX_CARGO_TARGET_DIR: target/feature-matrix
      USE_ZK_MOCK: "1"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4
        with:
          toolchain: 1.97.1

      - name: Run critical feature combinations
        run: bash ./scripts/test-feature-combinations.sh

  python-test:
    name: Python Tests (marty-biometrics)
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ['3.11', '3.12']
    defaults:
      run:
        working-directory: marty-biometrics
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: ${{ matrix.python-version }}

      - uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4
        with:
          toolchain: 1.97.1

      - name: Create virtualenv and install dependencies (Unix)
        if: runner.os != 'Windows'
        run: |
          python -m venv .venv
          source .venv/bin/activate
          pip install maturin[patchelf] pytest

      - name: Create virtualenv and install dependencies (Windows)
        if: runner.os == 'Windows'
        run: |
          python -m venv .venv
          .venv\Scripts\activate.bat
          pip install maturin pytest
        shell: cmd

      - name: Build Python package (Unix)
        if: runner.os != 'Windows'
        run: |
          source .venv/bin/activate
          maturin develop --features python

      - name: Build Python package (Windows)
        if: runner.os == 'Windows'
        run: |
          .venv\Scripts\activate.bat
          maturin develop --features python
        shell: cmd

      - name: Run Python tests (Unix)
        if: runner.os != 'Windows'
        run: |
          source .venv/bin/activate
          pytest python/tests/

      - name: Run Python tests (Windows)
        if: runner.os == 'Windows'
        run: |
          .venv\Scripts\activate.bat
          pytest python\tests\
        shell: cmd

  verification-python-test:
    name: Python/Rust Conformance (marty-verification)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: marty-verification
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: '3.11'
      - uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4
        with:
          toolchain: 1.97.1
      - name: Build native verification bindings
        run: |
          python -m venv .venv
          source .venv/bin/activate
          pip install 'maturin[patchelf]==1.14.1' pytest
          maturin develop
      - name: Run shared golden-vector tests
        run: |
          source .venv/bin/activate
          pytest python/tests/

  iso18013-python-test:
    name: Python/Rust Conformance (marty-iso18013)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: marty-iso18013
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: '3.11'
      - uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4
        with:
          toolchain: 1.97.1
      - name: Install native transport build dependencies
        run: sudo apt-get update && sudo apt-get install --yes libpcsclite-dev libdbus-1-dev pkg-config
      - name: Build native ISO bindings
        run: |
          python -m venv .venv
          source .venv/bin/activate
          pip install 'maturin[patchelf]==1.14.1' pytest
          maturin develop
      - name: Run native binding tests
        run: |
          source .venv/bin/activate
          pytest python/tests/

  wasm-test:
    name: WASM Tests (marty-biometrics)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: marty-biometrics
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4
        with:
          toolchain: 1.97.1

      - name: Install wasm-pack
        run: curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh

      - name: Build WASM (web target)
        run: wasm-pack build --target web --features wasm

      - name: Build WASM (nodejs target)
        run: wasm-pack build --target nodejs --features wasm

      - name: Build WASM (bundler target)
        run: wasm-pack build --target bundler --features wasm
